export type TimelineStep = {
  era: string;
  title: string;
  description: string;
};

export type BrechaItem = {
  title: string;
  description: string;
};

export type ObjetivoItem = {
  id: string;
  title: string;
  description: string;
};

export type Subservicio = {
  id: string;
  title: string;
  description: string;
  detail: string;
};

export type DeclaracionPillar = {
  label: string;
  description: string;
};

export type ValorItem = {
  id: string;
  name: string;
  description: string;
};
